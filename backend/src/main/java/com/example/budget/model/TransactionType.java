package com.example.budget.model;

/**
 * Enumeration representing the type of a transaction.
 * 
 * Used to distinguish between income (money received) and expense (money spent)
 * transactions in the budget application.
 */
public enum TransactionType {
    INCOME,
    EXPENSE
}

