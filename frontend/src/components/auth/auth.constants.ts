/**
 * Auth-related constants and regex.
 * Shared between LoginForm and RegisterForm to keep logic consistent.
 */

export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/

export const MIN_PASSWORD_LENGTH = 6
